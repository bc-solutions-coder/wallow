Wallow has been deployed to your cluster.

{{- if .Values.ingress.enabled }}

Access the application at:
  https://{{ .Values.ingress.host }}

{{- else }}

Access the application via port-forward:
  kubectl port-forward svc/{{ include "wallow.fullname" . }} 8080:{{ .Values.service.port }} -n {{ .Release.Namespace }}
  http://localhost:8080

{{- end }}

Health check:
  curl http://localhost:8080{{ .Values.health.readiness.path }}

Manage the release:
  helm upgrade {{ .Release.Name }} {{ .Chart.Name }} -n {{ .Release.Namespace }} -f values.yaml
  helm uninstall {{ .Release.Name }} -n {{ .Release.Namespace }}
