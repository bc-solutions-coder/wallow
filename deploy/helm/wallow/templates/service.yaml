apiVersion: v1
kind: Service
metadata:
  name: {{ include "wallow.fullname" . }}
  labels:
    {{- include "wallow.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "wallow.selectorLabels" . | nindent 4 }}
