apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "wallow.fullname" . }}
  labels:
    {{- include "wallow.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
