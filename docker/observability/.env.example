# Parent directory on your manually provisioned telemetry disk.
OBSERVABILITY_DATA_DIR=/srv/observability

# setup.py generates a private .env once. Keep the same secret on subsequent runs.
GARAGE_RPC_SECRET=
