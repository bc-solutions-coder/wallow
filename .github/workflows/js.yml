name: JS CI

on:
  pull_request:
    branches: [main]
    paths:
      - 'apps/**'
      - 'packages/**'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - 'pnpm-workspace.yaml'
      - 'tsconfig.base.json'
      - '.oxlintrc.json'
      - '.oxfmtrc.json'
      - '.nvmrc'
      - '.npmrc'
      - '.github/workflows/js.yml'
  push:
    branches: [main]
    paths:
      - 'apps/**'
      - 'packages/**'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - 'pnpm-workspace.yaml'
      - 'tsconfig.base.json'
      - '.oxlintrc.json'
      - '.oxfmtrc.json'
      - '.nvmrc'
      - '.npmrc'
      - '.github/workflows/js.yml'

concurrency:
  group: js-${{ github.head_ref || github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v7

      - name: Enable corepack
        run: corepack enable

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version-file: '.nvmrc'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm lint

      - name: Check formatting
        run: pnpm format:check

      # Workspace members resolve @bc-solutions-coder/sdk through its package.json
      # `exports` map, which points at dist/. Build the SDK first so dependent
      # members (apps/tanstack-min) can typecheck against the emitted .d.ts files.
      - name: Build SDK
        run: pnpm --filter @bc-solutions-coder/sdk build

      - name: Typecheck
        run: pnpm typecheck

      - name: Test
        run: pnpm test

      - name: Build
        run: pnpm build
