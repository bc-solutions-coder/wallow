/**
 * Browser-safe barrel for @bc-solutions-coder/web-shell.
 *
 * This entry is importable from client-side bundles (no Node APIs). It is a
 * placeholder at the scaffold stage (Wallow-0q2s.8.1) — the shared query-client
 * moves here in Wallow-0q2s.8.2. The marker export only exists so the package
 * builds and typechecks with a concrete export while the scaffold is wired up.
 */
export const WEB_SHELL_BROWSER_ENTRY = "@bc-solutions-coder/web-shell" as const;
