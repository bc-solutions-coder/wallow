{
  "name": "@wallow/example-tanstack-min",
  "version": "0.0.0",
  "private": true,
  "description": "Minimal TanStack Start-style BFF wiring for @wallow/sdk. Drop-in reference example — not published.",
  "type": "module",
  "scripts": {
    "build:client": "esbuild src/app.ts --bundle --format=esm --outfile=public/app.js",
    "build": "npm run build:client",
    "start": "tsx server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@wallow/sdk": "file:../..",
    "h3": "^1.13.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
