{
  // Typecheck shipped source without ambient Node types.
  // Environment values arrive as arguments; the consuming server owns process.env reads.
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "types": []
  },
  "include": ["src"],
  "exclude": ["src/**/*.test.ts"]
}
