{
  // Explicit entries ensure that each public subpath emits declarations.
  "extends": ["./tsconfig.json", "../../tsconfig.build.base.json"],
  "include": [
    "src/auth-origin.ts",
    "src/base-path.ts",
    "src/internal-origin.ts",
    "src/published-global.ts"
  ]
}
