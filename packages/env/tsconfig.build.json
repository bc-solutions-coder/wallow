{
  // Every entry listed explicitly: an unlisted module emits no .d.ts unless
  // another module here happens to import it (only auth-origin imports
  // published-global today).
  "extends": ["./tsconfig.json", "../../tsconfig.build.base.json"],
  "include": [
    "src/auth-origin.ts",
    "src/base-path.ts",
    "src/internal-origin.ts",
    "src/published-global.ts"
  ]
}
