{
  // Typecheck tests and build configuration with Node types.
  // These development files are not runtime package entries.
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "types": ["node"]
  },
  "include": ["src/**/*.test.ts", "vite.config.ts", "vitest.config.ts"]
}
