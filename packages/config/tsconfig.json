{
  // Typecheck the Vite configuration helpers directly.
  // This private package has no build step because other package builds import its source.
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // Config code runs in Node — `process.env`, `node:url`, `node:fs`.
    "types": ["node"]
  },
  "include": ["src"]
}
