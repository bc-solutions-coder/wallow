{
  // Typecheck shipped helpers without DOM or ambient Node types.
  // Tests and build configuration use tsconfig.node.json instead.
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ESNext"],
    "types": []
  },
  "include": ["src"],
  "exclude": ["src/**/*.test.ts"]
}
