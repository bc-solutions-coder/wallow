{
  // Emit declarations for the public entry using the shared build settings.
  // The JavaScript TypeScript compiler also supports the OpenAPI generator API.
  "extends": ["./tsconfig.json", "../../tsconfig.build.base.json"],
  "include": ["src/index.ts"]
}
