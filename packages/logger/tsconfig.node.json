{
  // Typecheck tests and build configuration with Node types.
  // These files are excluded from consumer runtime entries.
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "types": ["node"]
  },
  "include": ["src/**/*.test.ts", "src/**/*.test.tsx", "vite.config.ts", "vitest.config.ts"]
}
