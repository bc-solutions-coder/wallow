**status: active**

# Preserve resolved dependency inputs for publication

Issue #283. The publication preparation step must scan resolved dependencies with the
current vulnerability database without executing package installation or MSBuild under
publication control. The existing credential-free security job already restores and
scans the pnpm lockfile and generated NuGet lockfiles, checking coverage against its
inventory. Preserve those exact files and the inventory as a sealed immutable artifact.

Full-route registration and authorization now require `dependency-inputs` from the exact
producer run and attempt. Docs-only routes do not claim application dependency coverage.
The archive contains the existing pnpm/NuGet inputs and `inventory.json`; it contains no
package downloads or build outputs. The producer does not perform another restore.

Verification: run the full helper suite and Actionlint; exercise the archive/seal with
real generated lockfiles. Hosted export and a consumer that safely extracts, checks
inventory coverage, and rescans with current policy remain required. This slice alone
does not complete publication preparation or authorize any writer.

Local verification passed: all 105 helper tests, Actionlint, and archive/seal verification
over the pnpm lockfile plus 55 NuGet lockfiles generated by a real solution restore. The
archive was 607,602 bytes. Its synthetic run identity proves serialization only; it is
not an authorized hosted producer. Evidence: `/tmp/wallow-dependency-input-proof.json`.
