**status: active**

# Unified Plan — TanStack Start Adoption + SDK Streamlining

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan
> task-by-task. Phases are strictly ordered; tasks within a phase are ordered unless marked
> parallel-safe. Every phase ends with a hard gate — do not start the next phase on a red gate.

**Goal:** All three React apps become real TanStack Start applications consuming a
framework-neutral, mostly-generated SDK — no hand-rolled SSR host, no h3, no module-global
client singleton, no hand-written query layer — so a fresh fork gets a working, secure app from
"install the SDK, add `tanstackStart()`, mount the splat server routes, write feature screens."

**Architecture:** The SDK's server entry ports from h3 `EventHandler`s to web-standard
`Request → Response` handlers (its BFF/proxy/session core stays hand-written — that is the
product). Apps mount those handlers via Start file-based server routes and consume a
per-request `createWallowSdk()` client. `@tanstack/react-router-ssr-query` solely owns SSR
dehydration. The operation + query surface is generated by hey-api (`@hey-api/sdk` +
`@tanstack/react-query` plugins).

**Tech Stack:** .NET 10 (OpenAPI transformers), `@hey-api/openapi-ts@0.99.0`,
`@tanstack/react-start` + `@tanstack/react-router` (pinned exact, current latest),
`@tanstack/react-router-ssr-query`, Nitro (opt-in Vite plugin) for the Node/Docker server,
Vite 8, TanStack Query v5.

---

## 1. Sources, status, and numbering

This plan **merges and supersedes the sequencing of**:

- `0909-tanstack-start-migration-design.md` (the Start migration design) — mark
  **superseded** when this plan is accepted.
- `0928-frontend-and-sdk-streamlining-plan.md` (the SDK streamlining plan) — mark
  **superseded** when this plan is accepted.
- `0908-sdk-review.md` remains the **findings record** (not superseded; it is evidence, not a
  plan).

**Numbering rule for this document.** `0908-sdk-review.md` contains two independent numbered
lists that collide in the ranges the source plans cite. This plan always cites:

- **F1–F14** = the §2 security-findings table of 0908.
- **R1–R25** = the §5 recommendation list of 0908.

Never cite a bare number.

## 2. Verified facts that changed the source plans

Each item below was verified against the live repo, installed `node_modules`, or live library
docs on 2026-07-27 (repo scout + docs researcher). Where a source plan says otherwise, **this
section wins**.

| # | Fact | Impact |
|---|---|---|
| V1 | Installed `@tanstack/react-start@1.168.28` exposes **no** `createServerFileRoute`/`createServerRoute`, and whether the `server` option augmentation is usable at the installed pin is disputed between reviews. Current *docs* (latest) document `createFileRoute` with `server: { handlers }`. | Server routes require the **version bump first**; availability is confirmed by the Phase 3 spike (task 3.0), not assumed. 0928's "fix the bff-server.ts comment first, it is wrong today" is dropped — the comment is accurate for installed versions and the file dies in Phase 3. |
| V2 | No `ANY` handler key is documented for server routes — only explicit method keys (`GET`, `POST`, …) on splat routes. | All server-route tasks use explicit per-method handlers. If the spike finds `ANY` works, it may be used, but the plan does not depend on it. |
| V3 | Plain `vite build` under Start emits `dist/client` + `dist/server`. **`.output/server/index.mjs` exists only with the opt-in `nitro()` plugin** (`npm i nitro`, `nitro/vite`). No documented `PORT` contract for that path. | Dockerfiles/compose/Aspire changes target the Nitro output; the spike verifies boot, `PORT`, and signal handling in a container before any real app migrates. |
| V4 | hey-api 0.99.0 (already latest) has **no `@tanstack/query-core` plugin** — only framework-specific plugins (`@tanstack/react-query` et al.). | R12's "framework-agnostic generated query layer" is unachievable as stated. We use `@tanstack/react-query` and make the React Query peer **optional** (only `./query` consumers need it). Vue/Svelte consumers are out of scope until hey-api ships agnostic output. |
| V5 | Generated query keys are **flat single-object arrays** (`[{ _id, baseUrl, path }]`), invalidation via deep-partial object matching or opt-in `queryKeys: { tags: true }`. No hierarchical prefix factory. | The CLAUDE.md "all keys from the `queryKeys` factory" rule is **rewritten**, not preserved (task 4.7). Curated invalidation module uses `_id`/tags matching. |
| V6 | `throwOnError` is a **client-plugin** option (`@hey-api/client-fetch`); `responseStyle: 'data'` is an **sdk-plugin** option. | Config in task 4.1 places each correctly. |
| V7 | TanStack/router#7614 (Vite 8 dev-server 404) is **still open** and the installed plugin still has the bug; explicit `tanstackStart({ vite: { installDevServerMiddleware: true } })` (nested under `vite`, not top-level) bypasses it. | Every app's `vite.config.ts` sets it explicitly. Vite+ and a Vite 7 pin remain forbidden (they cause/mask the wrong problem). |
| V8 | `@tanstack/react-router-ssr-query@1.167.1` is the correct, current package (not `react-router-with-query`); it is not yet installed anywhere. `useSuspenseQuery`/loader prefetches SSR; plain `useQuery` does not run on the server. | Added per app in Phase 3. Screens relying on SSR'd data must use loader prefetch or suspense queries — checked per app during migration. |
| V9 | The SDK's h3 coupling is only **event-accessor helpers** in `server/handlers.ts` + `server/proxy.ts` (no h3 session utils anywhere); the app-side h3 coupling is app/router assembly + `toWebHandler`. | The h3-ectomy (Phase 2) is smaller than feared: port two files' accessor calls to `Request`/`Response`, delete the h3 dep. Apps keep a ~5-line temporary h3 shim until Phase 3 deletes their hosts. |
| V10 | CI's `e2e-tests` job is **disabled** (`ci.yml:322`, `if: false`) and only wallow-auth's suite is wired; the closed Start epic recorded "E2E 65/76" as the documented baseline. | The migration's safety net must be re-established first (task 3.0a): run suites locally, record the baseline, re-enable the job when green. |
| V11 | Open epic `Wallow-m5aq` (Base UI catalog rebuild) sweeps both apps' files in its later phases. | Hard sequencing rule: Phase 3/4 of this plan and m5aq's app-sweep phases must not run concurrently on the same app. |
| V12 | Start ships sessions (`getSession`/`sealSession`/…) and `createCsrfMiddleware`. | Confirmed available — and deliberately **not** used in the SDK (decision D6). |
| V13 | `request context`: `@tanstack/react-start/server` exports `getRequest`, `getRequestHeaders`, `getCookie`, etc.; global request middleware via `createStart(() => ({ requestMiddleware: [...] }))` in `src/start.ts` runs before server routes, SSR, and server functions. | This is the consumption site for SDK per-request wiring (D4). |

## 3. Decision log (collision resolutions)

- **D1 — h3 is removed from the SDK.** `packages/sdk/src/server/**` goes web-standard
  `Request → Response`. `iron-webcrypto` stays a **direct** dependency (this inverts 0928's
  "import iron through h3" small-win, which only made sense while h3 remained). Apps drop h3
  entirely in Phase 3 when their hosts are deleted.
- **D2 — SDK surface collapse comes AFTER the Start migration** (0909's ordering wins over
  0928's). Rationale: query-plugin adoption churns feature files; the migration churns app
  shells; separating them keeps each diff reviewable and each gate meaningful. The only SDK
  work that precedes migration is what the migration consumes: web-standard handlers,
  `createWallowSdk`, SSR-safe auth redirects, `createWallowBffServer`/passthrough presets
  (Phase 2).
- **D3 — `@tanstack/react-router-ssr-query` solely owns dehydration.** The SDK ships **no**
  dehydration/hydration code, ever. wallow-web's JSON-string workaround and the absent
  hydrate wiring in wallow-auth/minimal-app are all replaced by the integration.
- **D4 — one SSR request-context design.** The SDK ships per-request primitives
  (`createWallowSdk(options)`; no module-global state). Start's global request middleware is
  the only consumption site: it builds the per-request SDK from `getRequest()` and places it
  in router context. `setSsrRequestContextResolver`, `configureSsrClient`, and the
  AsyncLocalStorage seam are deleted in Phase 4. `resolveSsrInternalOrigin` logic survives as
  an SDK option (`internalOrigin`) because Start returns the browser-facing URL, which is
  wrong for container self-fetch.
- **D5 — no `createPassthroughProxy` extraction into web-shell.** The passthrough preset
  ships from the **SDK server entry** instead (task 2.7) and is consumed by server routes;
  web-shell's server layer is deleted wholesale.
- **D6 — Start's session/CSRF utilities are not used in the SDK.** The SDK stays portable;
  its session/CSRF core is the audited product. We steal the lesson only: explicit iron
  `ttl` (task 0.6).
- **D7 — query keys**: adopt generated flat keys + `queryKeys: { tags: true }`; keep one
  small curated `invalidations.ts` for subtree sweeps (matching on `_id`/tags). Rewrite the
  CLAUDE.md frontend-state rule in the same change (task 4.7).
- **D8 — server routes use explicit method keys** on splat routes (V2).
- **D9 — hosting is Nitro-on-Vite** (`nitro()` plugin), verified by the spike (V3).
- **D10 — epic sequencing**: this plan's Phases 3–4 must not overlap `Wallow-m5aq`'s
  app-sweep phases on the same app. Coordinate via beads before claiming either.
- **D11 — E2E safety net re-established before any app migrates** (V10).
- **D12 — no standalone "fix the comment" task.** `bff-server.ts:16-33` is accurate at
  installed versions (V1) and the file is deleted in Phase 3.
- **D13 — temporary app-side h3 shims in Phase 2 are acknowledged scaffolding**, ~5 lines
  per app, with a `// TEMPORARY until Phase 3` marker and a bead to delete them. This is the
  one place the plan accepts throwaway glue, and it exists so every phase gate stays green.

---

## Phase 0 — Security + independent de-risking

All tasks are independent of the migration and of each other unless noted. Backend tasks
follow `.claude/rules/CONVENTIONS.md` (explicit types, `[LoggerMessage]`, `dotnet format`
before commit) and run `./scripts/run-tests.sh <module>`.

### Task 0.1 — Delete `wallow-dev-client` (F1, R1)

**Files:** Modify `api/seed.json` (~line 183), `api/src/**/appsettings.json` (~line 78,
`FirstPartyClients`); sweep test fixtures + docs for references.
**Steps:** Remove the client entry and its `FirstPartyClients` registration → grep repo-wide
for `wallow-dev-client` and fix every reference → `./scripts/run-tests.sh Identity`.
**Commit:** `fix(identity)!: remove public wallow-dev-client seed client`

### Task 0.2 — Wire `IScopeSubsetValidator` into `AuthorizationController` (F2, R2)

**Files:** Modify `AuthorizationController` (Identity module); add integration test.
**Steps:** Write failing integration test: a plain `user` requesting
`roles.write users.manage` scopes must be rejected → wire the validator (mirror the
`ApiKeysController` usage) → test green → `./scripts/run-tests.sh Identity`.
**Commit:** `fix(identity): validate requested scopes against caller role in authorization endpoint`

### Task 0.3 — Explicit `"public": true` client flag + startup hard-fail (F3, R3)

**Files:** Modify `PreRegisteredClientOptions.cs` (~line 25) and client registration startup
path; `docker-compose.production.yml` env docs.
**Steps:** Failing test: registering a client with no secret and no explicit
`"public": true` must throw at startup → implement flag + hard-fail → migrate seed/compose
configs to declare the flag explicitly → tests green.
**Commit:** `fix(identity)!: require explicit public-client flag; fail closed on missing secret`

### Task 0.4 — Operator flag instead of `sa-` prefix check (F4, R4)

**Files:** `TenantResolutionMiddleware.cs:31-47,99-103`; service-account issuance
(`OpenIddictServiceAccountService.cs`).
**Steps:** Failing test: a tenant service account sending `X-Tenant-Id` for another tenant
is rejected → introduce an explicit operator claim/flag checked instead of the name prefix →
green.
**Commit:** `fix(identity): gate X-Tenant-Id override on operator flag, not client-id prefix`

### Task 0.5 — Call `isSafeReturnUrl` in the BFF login handler (F6, R5)

**Files:** `packages/sdk/src/server/handlers.ts:326-327,416` + `handlers.test.ts`.
**Steps:** Failing test: `/bff/login?returnTo=https://evil.example` must not round-trip to an
absolute foreign origin after callback → guard with the existing `isSafeReturnUrl` → green →
`pnpm --filter @bc-solutions-coder/sdk test`.
**Commit:** `fix(sdk): reject unsafe returnTo in BFF login handler`

### Task 0.6 — Explicit iron `ttl`; document Valkey as production-required (F7, R6)

**Files:** `packages/sdk/src/server/session.ts:55`, `store/cookie.ts:44-46`,
`docs/integrations/bff-pattern.md`.
**Steps:** Failing test: unsealing a sealed blob older than the session TTL must fail →
pass explicit `ttl` to seal/unseal derived from session config → document that the cookie
store is a dev default and Valkey is required in production (and why `destroy()` cannot
revoke cookie-store sessions) → green.
**Commit:** `fix(sdk): expire sealed session blobs cryptographically via iron ttl`

### Task 0.7 — Decide the global-`admin` design question (F5, R7) — **DECISION, needs owner**

Present the two options (realm superadmin as a distinct non-assignable role vs. tenant-scoped
admin only) with the `RolePermissionMapping.cs:19` / `UsersController.cs:142-178` evidence.
Implement whichever is chosen; failing test first (tenant admin attempts to grant global
admin cross-tenant).

### Task 0.8 — Tenant-filter integration test for compiled queries (0908 §"Unresolved")

**Files:** New integration test against `StoredFileRepository`/`ServiceAccountRepository`
(`TenantAwareDbContext.cs:35-38`).
**Steps:** Seed two tenants → run the compiled queries as tenant A → assert zero rows from
tenant B. This is a test-only task; if it fails, stop and treat as a critical bug.
**Commit:** `test(storage): prove tenant filtering on compiled queries`

### Task 0.9 — Delete the three `AnyRouter` annotations (parallel-safe, frontend)

**Files:** `apps/wallow-web/src/router.tsx`, `apps/wallow-auth/src/router.tsx`,
`apps/examples/minimal-app/src/router.tsx`.
**Steps:** Remove `: AnyRouter` from each `createRouter()` (restore inference) → run
`pnpm typecheck` → **fix every surfaced error** (latent `<Link>`/`useParams`/`navigate`
errors are the point; budget real time) → `pnpm check`.
**Commit:** `fix(web): restore router type inference by removing AnyRouter annotations`

### Task 0.10 — Silence the failing Prettier post-processor

**Files:** `packages/sdk/openapi-ts.config.ts`.
**Steps:** Verify the 0.99.0 option name for disabling output formatting (expected:
`output: { path, format: false }`) against installed types → set it → run the generator →
confirm no `openapi-ts-error-*.log` is produced and `git diff` on `src/generated` is
formatting-only or empty → delete the stale logs.
**Commit:** `chore(sdk): disable generator format pass (repo formatter is oxfmt)`

**PHASE 0 GATE:** `./scripts/run-tests.sh` all green · `pnpm check` green · findings F1–F4,
F6, F7 closed or explicitly deferred with owner sign-off.

---

## Phase 1 — Backend OpenAPI (unblocks all codegen)

### Task 1.1 — `operationId` transformer (R8)

**Files:** `api/src/Wallow.Api/Extensions/ServiceCollectionExtensions.cs:45-48` (alongside
the four existing transformers) + new transformer class + unit test.
**Steps:** Failing test: generated document has an `operationId` for every operation, equal
to `ControllerActionDescriptor.MethodInfo.Name` → implement → green.
**Commit:** `feat(api): emit operationId from controller action names`

### Task 1.2 — Typed success bodies (R9)

**Files:** The 67 operations lacking `[ProducesResponseType(typeof(T), 200)]` — enumerate by
diffing the regenerated spec for `200` responses without content schemas; commit per module.
**Commit (per module):** `fix(<module>): declare typed 200 responses`

### Task 1.3 — Exclude `Test Support` endpoints from the v1 document (R10)

**Commit:** `fix(api): exclude test-support endpoints from public OpenAPI document`

### Task 1.4 — Regenerate the SDK snapshot; absorb the one-time rename

**Files:** `packages/sdk/openapi/v1.json`, `packages/sdk/src/generated/**`, every app call
site using old path-derived names.
**Steps:** API running on 5001 →
`WALLOW_OPENAPI_URL=http://localhost:5001/openapi/v1.json pnpm --filter @bc-solutions-coder/sdk exec tsx scripts/generate.ts`
→ commit both snapshot and generated output → fix compile errors across
`packages/sdk/src/{auth-client,mfa-client,query}` and apps → `pnpm check`.
**Commit:** `feat(sdk)!: regenerate client with stable operationIds and typed responses`

### Task 1.5 — Rewrite `openapi-regen.test.ts` as an invariant (R11)

Assert "every operation in `openapi/v1.json` has an `operationId` and a typed 200" instead
of the dated snapshot assertion.
**Commit:** `test(sdk): enforce operationId + typed-success invariants on the spec`

**PHASE 1 GATE:** `openapi-drift.yml` green on a PR · `pnpm check` green.

---

## Phase 2 — SDK re-platform (what the migration consumes)

The SDK's h3 surface becomes web-standard. Apps stay green throughout via the D13 shims.

### Task 2.1 — Spike: web-standard handler port risks (timeboxed, 1 day)

**Steps:** In a scratch branch, port the logout handler + one proxy path to
`(request: Request) => Promise<Response>` → write tests proving: (a) **multiple `Set-Cookie`
headers survive** via `Headers.append` + `getSetCookie()` round-trip under Node 24; (b)
session **cookie chunking** (indexed cookies + stale-chunk clearing) works through the new
shape; (c) streaming bodies pass through the proxy untouched.
**Abort criteria:** if multi-`Set-Cookie` cannot be made reliable, stop and redesign before
touching Phase 2 proper.

### Task 2.2 — Port `server/handlers.ts` to web-standard

**Files:** `packages/sdk/src/server/handlers.ts` (446 lines) + `handlers.test.ts` (875).
**Steps:** Replace h3 accessors (`getCookie`/`setCookie`/`getQuery`/`sendRedirect`/…) with
`Request`/`Response`/`Headers` equivalents; handlers become
`(request: Request) => Promise<Response>` — tests drop `createApp`/`toWebHandler` and call
handlers directly with `new Request(...)`. Preserve behavior byte-for-byte where tests assert
it (redirect shapes, cookie attributes, RFC 7807 bodies).
**Commit:** `feat(sdk)!: BFF handlers accept web-standard Request/Response`

### Task 2.3 — Port `server/proxy.ts` to web-standard; fix the URL-construction shape (F11)

**Files:** `packages/sdk/src/server/proxy.ts` (699) + tests (1446).
**Steps:** Same porting rules. **Do not** change refresh-lock, 401-retry, cookie-challenge
detection, `Retry-After`, or RFC 7807 passthrough logic (0928 "correct as-is" list). While
in the file, replace `new URL(stripped, base)` (proxy.ts:601-603) with the concatenation
pattern from `auth-server.ts:150` + explicit path normalization, with a test for `//` and
`..` shapes.
**Commit:** `feat(sdk)!: API proxy accepts web-standard Request/Response`

### Task 2.4 — Remove h3 from the SDK; shim the apps

**Files:** `packages/sdk/package.json` (drop `h3`); app shims:
`apps/wallow-web/src/lib/bff-server.ts`, `apps/wallow-auth/src/lib/auth-server.ts`,
`apps/examples/minimal-app/src/lib/proxy-server.ts`.
**Steps:** Delete the h3 dep from the SDK → in each app, wrap the new web handlers back into
the existing h3 hosts via `defineEventHandler((e) => handler(toWebRequest(e)))`-style glue
marked `// TEMPORARY until Phase 3 deletes this host` → `pnpm build` (SDK first) →
`pnpm check`.
**Commit:** `feat(sdk)!: drop h3 dependency from the server entry`

### Task 2.5 — `createWallowSdk(options)` per-request factory (R15-shape)

**Files:** New `packages/sdk/src/create-sdk.ts` + tests; touches `src/client.ts`,
`src/runtime-config.ts`, `src/csrf.ts`, `src/ssr.ts` (deprecations only, deletion in
Phase 4).
**Steps:** Failing tests first: (a) two instances with different `baseUrl` do not bleed;
(b) CSRF interceptor registers exactly once per instance; (c) SSR instance forwards the
per-request cookie header; (d) no `/api` default — `baseUrl` is required or explicitly
defaulted by the caller. Implement over the generated `createClient()` with per-call
`{ client }` binding. Mark `configureBffClient`/`configureSsrClient`/`facade.ts` as
`@deprecated` pointing here.
**Commit:** `feat(sdk): add per-request createWallowSdk factory`

### Task 2.6 — SSR-safe `login()`/`logout()`

**Files:** `packages/sdk/src/auth.ts:28,35` + test.
**Steps:** Failing test: importing and calling `login()` in a Node environment must not
throw `location is not defined` at module scope, and must throw a descriptive error (or
no-op with guidance) when invoked server-side → guard `typeof location !== "undefined"` (do
NOT couple to Start's `createIsomorphicFn` — the SDK stays framework-neutral) → green.
**Commit:** `fix(sdk): make login/logout safe to import and call under SSR`

### Task 2.7 — Server presets: `createWallowBffServer` + `createApiPassthrough` (R16-shape, D5)

**Files:** New `packages/sdk/src/server/bff-server.ts`, `src/server/passthrough.ts` + tests;
export from `src/server/index.ts`.
**Steps:**
- `createWallowBffServer(config)` absorbs what every fork's `bff-server.ts` hand-assembles:
  env config load, store selection (`REDIS_URL` → Redis adapter/Valkey, else cookie store),
  handler + proxy construction — returning
  `{ handleBff(request), handleApi(request), handleHealth() }` (all web-standard).
- `createApiPassthrough({ apiInternalUrl, prefixes, forwardClientIp })` absorbs the
  triplicated reverse proxy (`auth-server.ts` 201 / `proxy-server.ts` 139 lines): forwarded
  headers, XFF append, `Set-Cookie` preservation, prefix allowlist **including
  `/.well-known/**`** (see the OIDC-discovery memory — omitting it broke discovery once).
- Tests port the strongest cases from the apps' existing host tests.
**Commit:** `feat(sdk): ship createWallowBffServer and createApiPassthrough presets`

**PHASE 2 GATE:** `pnpm check` green · SDK builds before apps · all three apps boot under
`pnpm dev` and wallow-auth's Playwright suite passes locally.

---

## Phase 3 — TanStack Start migration (app by app)

### Task 3.0 — Pre-flight: versions, spike, safety net

**3.0a — E2E baseline (D11).** Run `pnpm --filter ./apps/wallow-auth test:e2e` and
`pnpm --filter ./apps/wallow-web test:e2e` + the cross-app journey against `pnpm backend`;
record pass counts in the epic bead (last known baseline: 65/76). Fix or explicitly document
any regression **before** migrating. Re-enable `ci.yml`'s `e2e-tests` job (`if: false` at
`ci.yml:322`) as soon as it passes locally.

**3.0b — Version pin.** In `apps/examples/minimal-app` only: upgrade
`@tanstack/react-start` + `@tanstack/react-router` to current latest, **exact pins** (no
`^`); add `@tanstack/react-router-ssr-query` (exact); add `nitro` (exact). Do not touch the
other apps' pins yet.

**3.0c — Confirmation spike (blocking, timeboxed 1 day).** In minimal-app:
```ts
// vite.config.ts
import { tanstackStart } from "@tanstack/react-start/plugin/vite";
import { nitro } from "nitro/vite";
export default defineConfig({
  plugins: [
    tanstackStart({ vite: { installDevServerMiddleware: true } }), // V7 — required, not optional
    react(),
    nitro(),
    ...wallowStyles(),
  ],
});
```
Verify, in order: (1) `vite dev` SSRs a route (no #7614 404); (2) a file route with
`server: { handlers: { GET } }` executes (V1); (3) splat route `routes/api/$.ts` receives
`params._splat`; (4) `vite build` + `node .output/server/index.mjs` boots, serves hashed
assets, respects `PORT`, exits cleanly on SIGTERM — inside a container; (5) `wallowStyles()`
emits `styles.css` correctly under the Start plugin.
**Abort criteria:** if (1)/(2) fail at the pinned versions, STOP — reopen the version
analysis. Do **not** reach for Vite+, a Vite 7 pin, or a hand-rolled fallback host.

### Task 3.1 — Migrate `apps/examples/minimal-app`

**Files:**
- Rewrite: `vite.config.ts` (spike shape), `src/router.tsx`, `src/routes/__root.tsx`.
- Create: `src/routes/v1/$.ts`, `src/routes/connect/$.ts`, `src/routes/[.]well-known/$.ts`
  (exact filename per Start's escaping rules — verify in spike), `src/routes/health.ts`,
  `src/start.ts` (global request middleware).
- Delete: `dev-server.ts`/`server.ts` usage, `vite.ssr.config.ts`, `src/ssr.tsx`,
  `src/client.tsx`, `src/lib/proxy-server.ts` (139), `src/lib/proxy-paths.ts` — consume
  `createApiPassthrough` instead.
- Modify: `package.json` scripts (`dev` = `vite dev`, `build` = `vite build`,
  `start` = `node .output/server/index.mjs`), Dockerfile entrypoint.

**Steps:**
1. `router.tsx`: inferred return type (no annotation), per-request `QueryClient`,
   `setupRouterSsrQueryIntegration({ router, queryClient })`.
2. `__root.tsx`: route `head()` + `<HeadContent/>` + `<Scripts/>`; keep branding theme
   injection + `ReadyIndicator` (`data-app-ready`); add branded `notFoundComponent`.
3. Server routes delegate to `createApiPassthrough(...)` with explicit method keys (D8).
4. `src/start.ts`: request middleware builds `createWallowSdk` per request into router
   context.
5. `pnpm check` → boot under Aspire (`pnpm backend`) → manual smoke → Docker build + run.
**Commit:** `feat(examples)!: migrate minimal-app to TanStack Start`

### Task 3.2 — Migrate `apps/wallow-auth`

Same shape as 3.1 (pins from 3.0b, exact). Its passthrough covers `/v1/**`, `/connect/**`,
`/.well-known/**`, `/health` — preserve the prefix list exactly (memory: OIDC discovery +
JWKS must resolve). Delete `src/lib/auth-server.ts` (201) + `proxy-paths.ts`.
**Gate before commit:** `pnpm check` + `pnpm --filter ./apps/wallow-auth test:e2e`
(routes + backend login smoke) green; `./scripts/e2e.sh` green.
**Commit:** `feat(auth)!: migrate wallow-auth to TanStack Start`

### Task 3.3 — Migrate `apps/wallow-web`

Same shape, plus:
- `routes/api/$.ts`, `routes/bff/$.ts`, `routes/health.ts` → `createWallowBffServer`
  handlers; delete `src/lib/bff-server.ts` (153, incl. the V1 comment), `proxy-topology.ts`.
- Replace the JSON-string dehydrate workaround (`router.tsx:18-65`) with ssr-query (D3);
  verify SSR'd screens use loader prefetch or suspense queries (V8).
- Request middleware replaces `src/lib/wallow-sdk.ts` + `ssr-origin.ts` usage
  (`internalOrigin` option carries `WALLOW_WEB_INTERNAL_URL` semantics, D4).
- Dashboard route guards keep working (claim helpers arrive in Phase 4 — don't block on
  them).
**Gate:** `pnpm check` + wallow-web e2e + **cross-app journey**
(`pnpm --filter ./apps/wallow-web test:e2e:cross-app`) green.
**Commit:** `feat(web)!: migrate wallow-web to TanStack Start`

### Task 3.4 — Delete the hand-rolled platform

**Files:**
- Delete: `packages/web-shell/src/server/**` (dev-server 263, standalone-host 209,
  vite-presets 84, index 20 + 1,014 test lines), `src/static-assets.ts` (98+88).
- Keep: `src/query-client.ts` (`createQueryClient`) and the browser barrel.
- Modify: web-shell `package.json` exports (drop `./server`), its CLAUDE.md;
  remove `@tanstack/router-cli` + `tsr.config.json` + `routes:generate` scripts from all
  apps (Start plugin owns codegen); `route-tree-drift.yml` repoints at plugin codegen and
  **adds minimal-app's routeTree to the guarded paths** (today it is unguarded);
  `docker/docker-compose.test.yml` image commands; Aspire AppHost Node resources (verify
  `pnpm dev` still works as the launch command).
**Gate:** full `pnpm check` + `./scripts/e2e.sh` + cross-app journey; Aspire boot smoke.
**Commit:** `feat(web-shell)!: delete the hand-rolled SSR host runtime`

**PHASE 3 GATE:** all of task 3.4's gate · CI `e2e-tests` job enabled and green · Docker
production compose boots all three apps.

---

## Phase 4 — Collapse the hand-written SDK surface

### Task 4.1 — Generated query layer (R12-shape, V4–V6)

**Files:** `packages/sdk/openapi-ts.config.ts`, `packages/sdk/package.json`.
```ts
plugins: [
  { name: "@hey-api/client-fetch", runtimeConfigPath: "./src/runtime-config", throwOnError: true },
  "@hey-api/typescript",
  { name: "@hey-api/sdk", responseStyle: "data" },
  { name: "@tanstack/react-query", queryOptions: true, queryKeys: { tags: true }, mutationOptions: true },
]
```
Move `@tanstack/react-query` to **optional** peer. Regenerate; `./query` re-exports the
generated artifacts + the curated `invalidations.ts`.
**Commit:** `feat(sdk)!: generate the TanStack Query surface for all operations`

### Task 4.2 — `WallowError` unification; delete both unwraps + `MfaUnwrap`

**Files:** `packages/sdk/src/facade.ts`, `auth-client.ts:313-321`,
`apps/wallow-web/src/features/mfa/errors.ts:38-45`, `mfa-client.ts`.
With `throwOnError: true` + `responseStyle: 'data'`, a response interceptor converts RFC 7807
bodies (code from `extensions.code`, `UNKNOWN` fallback) into `WallowError` for every
operation. Both apps consume `WallowError` only.
**Commit:** `feat(sdk)!: unify error contract on WallowError via generated client`

### Task 4.3 — `auth-client.ts` (376) → `auth-extras.ts` (~70) (R14)

Keep only: `getCurrentUser` 401-softening, space-joined `scopes` shaping, `clientId`
key-omission guard. Delete `mfa-client.ts` hand-written response interfaces (now generated).
**Commit:** `feat(sdk)!: collapse auth-client to auth-extras`

### Task 4.4 — Delete `src/query/` slices + singleton machinery; migrate app call sites

**Files:** Delete `src/query/{organizations,inquiries,apps,mfa,auth,keys,user,bootstrap,settings}.ts`
(~773), `src/client.ts` singleton config, `src/runtime-config.ts` baked defaults,
`src/facade.ts` `createConfiguredOnce`, `src/ssr.ts` global resolver; delete app shims
`apps/wallow-web/src/lib/wallow-sdk.ts`, `apps/wallow-auth/src/lib/wallow-auth-sdk.ts`,
`apps/examples/minimal-app/src/lib/sdk.ts`.
**Steps:** Migrate every app call site to generated `{op}Options()` /
`{op}Mutation()` + `invalidations.ts`; then delete. One commit per app, then the deletion
commit. This is the largest mechanical diff of the plan — land behind green
`pnpm check` + e2e at each commit.
**Commits:** `refactor(web): consume generated query options` ·
`refactor(auth): consume generated query options` ·
`feat(sdk)!: delete hand-written query layer and global client singleton`

### Task 4.5 — Claim helpers (R18)

`isAdmin`/`hasRole`/`getRoles` in the browser entry + adopt in
`apps/wallow-web/src/routes/dashboard/route.tsx` guards.
**Commit:** `feat(sdk): add claim helpers; stop hand-parsing roles in route guards`

### Task 4.6 — Optional QOL (do if cheap, skip if not)

Router/Query devtools gated to dev builds in all three apps; hey-api `zod` plugin evaluation
(runtime validation at the BFF boundary) — spike only, adopt by separate decision.

### Task 4.7 — Rewrite the rules and docs (same change as the code they describe)

**Files:** root `CLAUDE.md` (frontend state boundary — new query-key rule per D7),
`docs/development/frontend-state.md`, `packages/sdk/CLAUDE.md` (entries, regen, new factory,
web-standard server), `docs/integrations/bff-pattern.md`, `docs/integrations/typescript-sdk.md`,
`docs/getting-started/frontend-setup.md`, `docs/getting-started/fork-guide.md` (R25: BFF-only
stated honestly as policy over the ranked BCP).
**Commit:** `docs: rewrite frontend/SDK integration guides for Start + generated SDK`

**PHASE 4 GATE:** `pnpm check` · all e2e suites + cross-app journey · `pnpm --filter
@bc-solutions-coder/sdk build` consumed cleanly by a scratch out-of-repo install (fork smoke:
follow the fork guide from scratch against the built tarball).

---

## Phase 5 — Automation, hardening, cleanup (parallel-safe tracks)

- **5.1 (R20)** Build-time OpenAPI emit spike (`OpenApiDocumentsDirectory`); resolve the
  `/alive`+`/events` delta vs the byte-compare drift check before switching
  `openapi-drift.yml` off its Postgres/Valkey stack.
- **5.2 (R21)** Auto-regen on backend merge committing `feat(sdk):` (release-please +
  `sdk-v*` publish loop is already configured — close the last mile).
- **5.3 (R19)** Credential/transport split (cookie+CSRF vs DPoP bearer) — **before any React
  Native work starts**; otherwise schedule freely.
- **5.4** Config-text test cleanup (~2,800 lines): replace `package-scaffold.test.ts` ×3 with
  `publint` + `@arethetypeswrong/cli` in CI; drop the Dockerfile-text spec in favor of the
  existing `docker-images-app` CI build; delete workflow-text + one-time-migration specs.
  File-by-file — `service-identity.test.ts` and route-codegen specs mix real behavioral
  assertions in.
- **5.5** Backend guardrails (R22 cross-tenant test gate, R23 `FallbackPolicy`, R24 audience
  restriction, F8/F9/F10/F12/F13 remediations) — own track, backend-only.
- **5.6** Hygiene: `packages/testing` `deepMerge` → `mergeConfig`; `packages/sdk`
  `@types/node ^24` + `typescript` pin alignment with apps; `packages/ui` `clsx` +
  `tailwind-merge` only if the override bug is actually hit.

---

## Sequencing summary

```
Phase 0 (security + AnyRouter + format)        — independent, start immediately
Phase 1 (operationId/typed bodies/regen)       — after 0.10; gates 2 and 4
Phase 2 (SDK web-standard + factory + presets) — after 1; gates 3
Phase 3 (Start migration, app by app)          — after 2; 3.0 spike gates 3.1–3.4
Phase 4 (surface collapse + docs/rules)        — after 3
Phase 5 (automation/hardening/cleanup)         — 5.1/5.2 after 1; 5.4/5.5/5.6 anytime; 5.3 before RN
```

Cross-epic: no overlap with `Wallow-m5aq` app-sweep phases on the same app (D10).

## Risks

| Risk | Mitigation |
| --- | --- |
| Server routes don't work at pinned latest (V1 dispute) | 3.0c spike with hard abort criteria; nothing app-shaped starts until it passes |
| Nitro output under Docker/Aspire (PORT, signals) | Spike item 4; minimal-app carries the risk first |
| Multi-`Set-Cookie`/cookie-chunking regression in the h3-ectomy | 2.1 spike with abort criteria; port the strongest existing tests |
| `AnyRouter` removal surfaces a large latent-error batch | It's Phase 0 on purpose — fix before any file churn hides attribution |
| Generated-key adoption breaks invalidation semantics silently | `invalidations.ts` is curated + tested; rule rewrite lands in the same PR (D7) |
| Two epics churning the same app files | D10 sequencing rule, enforced via beads |
| E2E safety net was dark (CI job disabled) | 3.0a re-baselines and re-enables before migration |
| Version drift between plan-time and execution-time docs | Exact pins on day one; verify APIs against pinned versions' docs, not memory |

## Explicitly not doing

- Vite+ adoption, or pinning Vite 7 (both address the wrong problem — V7).
- Start's session/CSRF inside the SDK (D6).
- An SDK dehydration preset (D3).
- `createPassthroughProxy` in web-shell (D5 — it ships from the SDK instead).
- A standalone `bff-server.ts` comment fix (D12).
- Polishing the hand-rolled SSR kit, or deprecate-in-place for web-shell's server layer
  (rejected in 0909).
- Framework-agnostic generated query output (impossible at hey-api 0.99 — V4; revisit when
  hey-api ships it).

## Execution notes

- Turn this plan into beads with `spec-to-beads` (epic + per-phase features + per-task
  leaves; dependencies per the sequencing summary). No open beads currently cover this work.
- On acceptance: set this file's status to `active` execution reference, mark 0909 and 0928
  `superseded` (edit only their status lines), keep 0908 `active` as the findings record.
- Every phase-gate commit follows Conventional Commits (breaking SDK/app changes are `!`).
- Session completion discipline applies per CLAUDE.md (quality gates, `bd dolt push`,
  `git push`).
